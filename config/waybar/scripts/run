#!/usr/bin/env bash

set -u

running=true

stop() {
	running=false
	pkill -x waybar 2> /dev/null || true
}

trap stop INT TERM

while $running; do
	waybar
	status=$?

	if ! $running; then
		break
	fi

	sleep 1
done
