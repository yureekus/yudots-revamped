// Add personal window and layer rules here.
// This file loads after `rules.kdl`.

// Prevent apps from opening directly in fullscreen mode.
window-rule {
    open-fullscreen false
}

// Focus newly opened windows, including app activations from browser links/notifications.
window-rule {
    open-focused true
}
